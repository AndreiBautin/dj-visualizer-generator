# .NET
backend/**/bin/
backend/**/obj/
*.user

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/playwright-report/
frontend/test-results/
frontend/blob-report/

# run-standalone.bat copies the frontend build here at run time (mirrors the Dockerfile's
# COPY --from=frontend /src/dist ./wwwroot) - rebuilt every run, never committed.
backend/src/Api/wwwroot/

# Env
.env

# Local dev job storage. Unanchored on purpose: the Api and Worker each fall back to a
# jobs-data directory beside their own content root when Jobs__RootPath is unset, so uploaded
# audio and artwork can land in more than one place in the tree.
jobs-data/

# Local dev logs
*.log

# Editor / OS
.vs/
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db
